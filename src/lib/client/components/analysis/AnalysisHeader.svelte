<script lang="ts">
</script>

<header class="analysis-hero flip7-card accent-teal">
	<div class="hero-left">
		<div class="hero-badge">
			<span class="badge-icon">📊</span>
			<span class="badge-text">FINANCIAL INTELLIGENCE</span>
		</div>
		<h1 class="hero-title">MONEY INTELLIGENCE & ANALYSIS</h1>
		<p class="hero-subtitle">
			Understand where your money is going, track cash flow, and discover data-driven financial signals for the selected period.
		</p>
	</div>
</header>

<style>
	.analysis-hero {
		position: relative;
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: var(--space-lg, 16px);
		padding: 18px 24px;
		background: var(--color-surface, #ffffff);
		border: 1px solid var(--color-hairline, rgba(20, 48, 46, 0.08));
		border-radius: 24px;
		box-shadow: var(--shadow-sm, 0 1px 3px rgba(0, 0, 0, 0.04));
		margin-bottom: var(--space-md, 16px);
		overflow: visible;
	}

	.hero-left {
		display: flex;
		flex-direction: column;
		gap: 2px;
	}

	.hero-badge {
		display: inline-flex;
		align-items: center;
		gap: 6px;
		padding: 3px 10px;
		background: var(--color-teal-bg, rgba(43, 168, 162, 0.12));
		border-radius: var(--radius-pill, 999px);
		width: fit-content;
		margin-bottom: 2px;
	}

	.badge-icon {
		font-size: 12px;
	}

	.badge-text {
		font-family: var(--font-display);
		font-size: 11px;
		font-weight: 800;
		color: var(--color-teal-base, #2ba8a2);
		letter-spacing: 0.12em;
	}

	.hero-title {
		font-family: var(--font-display);
		font-size: clamp(20px, 2.4vw, 26px);
		font-weight: 800;
		color: var(--color-text-title, #14302e);
		margin: 0;
		letter-spacing: -0.02em;
		line-height: 1.15;
	}

	.hero-subtitle {
		font-size: var(--font-size-sm, 14px);
		color: var(--color-text-subtitle, #5c7a78);
		margin: 2px 0 0 0;
	}
</style>



