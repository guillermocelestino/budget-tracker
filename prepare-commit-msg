#!/bin/bash
COMMIT_MSG_FILE=$1
COMMIT_SOURCE=$2

if [ -z "$COMMIT_SOURCE" ]; then
  FILES=$(git diff --cached --name-only 2>/dev/null | wc -l | tr -d ' ')
  echo "" >> "$COMMIT_MSG_FILE"
  echo "# Changes: $FILES file(s)" >> "$COMMIT_MSG_FILE"
fi
